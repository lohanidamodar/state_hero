name: state_hero
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  build_context: ^2.0.4
  http: ^0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_automation: ^1.4.0

flutter:
  uses-material-design: true
